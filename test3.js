const fs = require('fs'); const js = fs.readFileSync('public/app.js', 'utf16le'); const renderBoardCode = js.split('function renderBoard(data) {')[1].split('function ')[0]; const lines = renderBoardCode.split('\n'); lines.forEach((l, i) => { if (l.match(/([a-zA-Z0-9_]+)\.(innerHTML|innerText|classList|style)/) && !l.includes('?.') && !l.includes('document.body') && !l.includes('document.getElementById')) { console.log((i+1) + ': ' + l.trim()); } });
